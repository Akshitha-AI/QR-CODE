# Agent Notes

This repository contains a Flask QR code generator with a static frontend.

## Useful Commands

- `python src/backend/app.py`
- `pytest --cov=src/backend tests`
- `docker compose up --build`

Generated runtime files are stored in `generated/` and `data/history.json`.
